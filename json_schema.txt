{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "performer rotation",
	"description": "rotaion data of the performer",
    "type": "object",
    "properties": {
	"performerId": {
		"type": "number"
		},
        "rotation": {
		"title": "rotation data",
		"type": "array",
		"items":{
			"title":"node",
			"type": "object",
			"properties": {
				"nodeName":{"type": "string",
					"description": "the standard body part"},
				"x": {"type": "number",
			    	  "description": "the rotation angle of x-axis"},
				"y": {"type": "number",
			    	  "description": "the rotation angle of y-axis"},
				"z": {"type": "number",
			     	 "description": "the rotation angle of z-axis"}
				},
			"required": ["node","x", "y", "z"]
       		 	}
    		}
	},
  "required": ["performerId","rotation"]
}
